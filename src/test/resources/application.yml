spring:
  main:
    banner-mode: OFF
  datasource:
    url: jdbc:postgresql://localhost:5432/home-4-you
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: password
logging:
  level:
    org:
      springframework: OFF
    root: OFF
